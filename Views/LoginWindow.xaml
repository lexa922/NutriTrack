<Window x:Class="NutriTrack.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:NutriTrack.Views"
        mc:Ignorable="d"
        Title="NutriTrack - Вхід" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid>
        <Border Background="White" 
                CornerRadius="15" 
                Padding="30" 
                Margin="30" 
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.1" ShadowDepth="2"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="NutriTrack" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="#4CAF50" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,10"/>
                
                <TextBlock Text="Увійдіть у свій профіль" 
                           Foreground="Gray" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,25"/>
                
                <TextBlock Text="Логін" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" 
                         Margin="0,0,0,15">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                
                <TextBlock Text="Пароль" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" 
                         Margin="0,0,0,10">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="Red" 
                           FontSize="12" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,10"
                           TextWrapping="Wrap"/>
                
                <Button Content="Увійти" 
                        Command="{Binding LoginCommand}"
                        Background="#4CAF50" 
                        Foreground="White" 
                        FontWeight="Bold" 
                        Padding="12" 
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,10,0,10">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
                <Button Content="Ще не маєте акаунту? Створити" 
                        Command="{Binding ShowRegisterCommand}"
                        Background="Transparent" 
                        Foreground="#2196F3" 
                        BorderThickness="0"
                        Cursor="Hand"
                        FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
